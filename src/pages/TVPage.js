import React from 'react';
import TVList from '../components/TVList';

const TVPage = () => {
    return (
        <div>
            < TVList />
        </div>
    );
};

export default TVPage;