import React from 'react';
import DataFetcher from '../components/DataFetcher';

const Home = () => {
    return (
        <div>
            < DataFetcher />
            Home page is now  available
        </div>
    );
};

export default Home;